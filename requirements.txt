discord.py>=2.4,<3
asyncpg>=0.29,<0.30
python-dotenv>=1.0,<2
# Job di calcolo del grafo: igraph e non NetworkX, per la memoria compatta
# su una droplet da 1 GB condivisa con bot e Postgres (architettura.md 3).
python-igraph>=0.11,<0.12
# Community detection: leidenalg gira su igraph, quindi la dipendenza pesante
# e' gia' in casa. Leiden e non Louvain: garantisce community ben connesse
# (architettura.md 3).
leidenalg>=0.10,<0.11
# API di sola lettura sugli aggregati (api/). uvicorn con 1 worker: piu' worker
# moltiplicherebbero la RAM su una macchina che non ha CPU da parallelizzare
# (architettura.md, sezione Hosting).
fastapi>=0.115,<0.120
uvicorn>=0.30,<0.35
pydantic>=2.7,<3
# Dashboard server-rendered (dashboard/): Jinja2 per i template, httpx come
# client verso l'API. Non Streamlit: 54 MB contro 181 misurati, e il login
# Discord non ci si scrive (docs/architettura/dashboard.md 2).
jinja2>=3.1,<3.2
httpx>=0.27,<0.29
# Firma del cookie di sessione del login Discord (SessionMiddleware di Starlette
# la importa). Firmato, non cifrato: in sessione non c'e' nessun token
# (docs/architettura/dashboard.md 3).
itsdangerous>=2.1,<3
# Il database dei fusi orari per zoneinfo. Lo usa la sola vista Stato, che mostra
# le date in Europe/Rome invece che in UTC (dashboard.md 4): e' l'unica pagina
# il cui lettore e' un amministratore, non qualcuno che confronta una riga con
# un log. Non e' una dipendenza di comodo — senza, ZoneInfo("Europe/Rome") alza
# ZoneInfoNotFoundError su Windows sempre, e su un'immagine slim ogni volta che
# il database di sistema non c'e'. Il fallimento e' rumoroso e immediato
# (all'avvio del processo, non alla prima pagina), ma non e' un motivo per
# lasciarlo dipendere dalla piattaforma.
tzdata>=2024.1

# Dipendenze di sviluppo, NON installate nell'immagine Docker: il bot e il
# job girano su una droplet da 1 GB e non hanno motivo di portarsi dietro un
# framework di test.
-r requirements.txt
pytest>=7.4,<9
# httpx (richiesto da fastapi.testclient) e' ora in requirements.txt: la
# dashboard lo usa a runtime come client verso l'API.
# Letto dai test che ispezionano docker-compose.yml (test_code_version.py,
# test_dashboard_deploy.py). Mancava: quei test fallivano con ModuleNotFoundError
# su un ambiente installato da questo file.
pyyaml>=6,<7

# Dipendenze di sviluppo, NON installate nell'immagine Docker: il bot e il
# job girano su una droplet da 1 GB e non hanno motivo di portarsi dietro un
# framework di test.
-r requirements.txt
pytest>=7.4,<9
# Client HTTP per i test dell'API (fastapi.testclient lo richiede).
httpx>=0.27,<0.29
